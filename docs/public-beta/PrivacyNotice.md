# Privacy Notice

KnowledgeOS is offline-first and user-controlled. Optional diagnostics are disabled by default and require consent. Diagnostic packages must exclude document content, annotations, AI prompts and responses, tokens, credentials, personal paths, and authorization headers.

Remote AI is used only when enabled and when the applicable context policy permits remote transfer. Users may delete local application data and revoke credentials through the operating system and KnowledgeOS settings.
