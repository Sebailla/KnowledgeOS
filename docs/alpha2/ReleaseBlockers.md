# Release Blockers

The release gate fails for open P0 or P1 defects. The repository does not include real evaluator defect data in the Sprint 047 input; `backlog.example.json` is a schema example and must not be treated as field evidence.
