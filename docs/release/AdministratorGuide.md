# Administrator Guide

Administrators operate the Sync Server, Master Library and PostgreSQL containers, maintain separate persistent volumes, monitor health endpoints, rotate logs, perform backups and test restoration. Secrets must be supplied externally and never committed or embedded in artifacts.
