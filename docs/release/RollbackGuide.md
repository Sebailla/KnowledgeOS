# Rollback Guide

Stop all components, collect sanitized diagnostics, restore the pre-upgrade database and authoritative-file backup, deploy the previous compatible artifacts, restart servers before clients and validate checksums, checkpoints, library counts and synchronization convergence.
