# Security and Privacy

KnowledgeOS is offline-first. Diagnostics are opt-in and sanitized. Credentials belong in Keychain or external deployment secrets. TLS is required outside local development. Imported packages, deep links and file paths are validated. Remote AI use requires an allowed context policy.
