# Backup and Recovery Guide

Back up PostgreSQL and authoritative files as one recovery point while storing them in separate archives. Record checksums and release metadata. Recovery is successful only after database restore, file restore, health checks, library integrity checks and cross-device convergence.
