import assert from "node:assert/strict";import {SyncHttpServer} from "../dist/index.js";class Plans{constructor(){this.m=new Map()}async save(x){this.m.set(x.planId,x)}async get(x){return this.m.get(x)}}class C{constructor(){this.m=[]}async attach(p,v,o){this.m.push(v)}async listByPlan(){return this.m}}class L{async tryAcquire(){return true}}const s=new SyncHttpServer({plans:new Plans(),checkpoints:new C(),leases:new L()},{host:"127.0.0.1",port:0,ownerId:"s1",leaseMilliseconds:60000});const a=await s.start();try{const plan={planId:"plan1",direction:"master-to-local",masterRevision:2,localRevision:1,transfers:[],unchanged:0};let r=await fetch(`http://${a.address}:${a.port}/v1/sync/plans`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(plan)});assert.equal(r.status,201);r=await fetch(`http://${a.address}:${a.port}/v1/sync/plans/plan1`);assert.equal(r.status,200);r=await fetch(`http://${a.address}:${a.port}/v1/sync/plans/plan1/lease`,{method:"POST"});assert.equal(r.status,200);console.log(JSON.stringify({flow:"sync-http-plan-progress-lease",status:"passed"}));}finally{await s.stop()}
