{
  "name": "@knowledgeos/sync-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/server.test.js dist-test/test/protocol-server.test.js",
    "validate": "pnpm typecheck"
  },
  "dependencies": {
    "@knowledgeos/sync": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
