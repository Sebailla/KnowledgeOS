node_modules/
.turbo/
dist/
build/
.next/
coverage/
DerivedData/
.swiftpm/
.build/
.env
.env.*
!.env.example
.DS_Store
# Local application data and runtime state. These are written by
# the macOS application and the Core host at runtime; they must not
# be tracked because they contain user-specific data and machine
# state that differs between developer machines.
.knowledgeos-data/
.knowledgeos/
