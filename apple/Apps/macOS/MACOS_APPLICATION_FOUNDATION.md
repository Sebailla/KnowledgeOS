# macOS Application Foundation

**Version:** 1.0.0  
**Status:** Foundation complete

The application is implemented as a native SwiftUI executable package.

The current service adapters are in-memory boundaries. Production adapters
for the TypeScript Core, local library runtime and Master Library services
must be connected through explicit bridge packages rather than direct UI
dependencies.
