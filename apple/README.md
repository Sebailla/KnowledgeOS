# Apple Workspace

Native macOS, iPhone and iPad applications use SwiftUI with AppKit/UIKit integration where required. Reusable Apple modules are Swift packages. Wire contracts are generated from `packages/contracts/schemas`; they are not handwritten separately.
