export type PersonalKnowledgeEventKind="item-upserted"|"item-deleted"|"relation-upserted"|"relation-deleted"|"snapshot-created";
export interface PersonalKnowledgeEvent {readonly eventId:string;readonly ownerId:string;readonly sequence:number;readonly kind:PersonalKnowledgeEventKind;readonly subjectId:string;readonly occurredAt:string;readonly payload:Readonly<Record<string,unknown>>;}
export interface PersonalKnowledgeEventStore {append(event:PersonalKnowledgeEvent):Promise<void>;listAfter(ownerId:string,sequence:number,limit:number):Promise<readonly PersonalKnowledgeEvent[]>;}
export class InMemoryPersonalKnowledgeEventStore implements PersonalKnowledgeEventStore {private values:PersonalKnowledgeEvent[]=[];async append(e:PersonalKnowledgeEvent){if(this.values.some(v=>v.ownerId===e.ownerId&&v.sequence===e.sequence))throw new Error("Duplicate owner sequence");this.values.push(e);this.values.sort((a,b)=>a.sequence-b.sequence);}async listAfter(o:string,s:number,l:number){return this.values.filter(v=>v.ownerId===o&&v.sequence>s).slice(0,l);}}
