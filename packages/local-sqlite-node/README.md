# @knowledgeos/local-sqlite-node

Real Node.js SQLite runtime based on `node:sqlite`.

## Includes

- WAL mode;
- foreign keys;
- busy timeout;
- immediate transactions;
- nested savepoints;
- rollback;
- persistent migrations;
- WAL checkpoints;
- deterministic close.
