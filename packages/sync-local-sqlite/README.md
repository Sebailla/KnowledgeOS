# @knowledgeos/sync-local-sqlite

SQLite persistence for Master → Local transfer descriptors and resumable checkpoints.

State survives application restarts and is stored in the same Local Library database.
