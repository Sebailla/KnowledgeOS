export class LocalMaintenanceRunner { constructor(private readonly publications:any,private readonly integrity:any,private readonly repair:any,private readonly planner:any,private readonly eviction:any,private readonly checkpoint:{checkpoint():void},private readonly disk:{availableBytes():Promise<number>},private readonly clock:{nowIso():string}){} async run(localLibraryId:string,policy:any){const rows=await this.publications.list(localLibraryId); const available=await this.disk.availableBytes(); const results=[await this.integrity.run(localLibraryId),await this.repair.run(localLibraryId),{...this.planner.plan(rows,policy),availableBytes:available,at:this.clock.nowIso()},await this.eviction.run(localLibraryId)]; this.checkpoint.checkpoint(); return results;} }
