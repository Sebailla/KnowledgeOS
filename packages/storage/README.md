# @knowledgeos/storage

Capa de persistencia agnóstica para KnowledgeOS.

Incluye providers, sessions, transacciones, optimistic concurrency,
migraciones, integración con `UnitOfWork` y un provider in-memory de referencia.

Los adaptadores SQLite y PostgreSQL se implementan en paquetes separados.
