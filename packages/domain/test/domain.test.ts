import assert from "node:assert/strict"; import test from "node:test";
import type { CorrelationId, EventId, IsoTimestamp, KnowledgeObjectId, LocalLibraryId, SourceItemId, VersionId } from "@knowledgeos/domain-types";
import { Acquisition, DomainError, KnowledgeObject, LocalLibrary } from "../src/index.js";
const version="version:test" as VersionId; const event={type:"test.event",eventId:"event:test" as EventId,contractVersion:"5.0.0" as never,aggregateId:"aggregate:test",aggregateVersionId:version,correlationId:"correlation:test" as CorrelationId,occurredAt:"2026-08-01T00:00:00.000Z" as IsoTimestamp,payload:{}};
test("knowledge object requires title",()=>{assert.throws(()=>KnowledgeObject.register({id:"knowledge-object:test" as KnowledgeObjectId,title:"",authority:{scope:"local-library",kind:"system"},provenance:[],status:"registered"},event),DomainError);});
test("library rejects duplicate membership",()=>{const lib=LocalLibrary.rehydrate({id:"local-library:test" as LocalLibraryId,name:"Local",memberships:[]});const m={localLibraryId:lib.id,knowledgeObjectId:"knowledge-object:test" as KnowledgeObjectId,sourceItemId:"source-item:test" as SourceItemId,sourceVersionId:version,state:"available" as const};lib.addMembership(m,event);assert.throws(()=>lib.addMembership(m,event),DomainError);});
test("acquisition progress is monotonic",()=>{const a=Acquisition.rehydrate({id:"acquisition:test" as never,publicationId:"publication:test" as never,targetLocalLibraryId:"local-library:test" as LocalLibraryId,status:"transferring",transferredBytes:100,totalBytes:200});assert.throws(()=>a.reportProgress(50,event),DomainError);});
