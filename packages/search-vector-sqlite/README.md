# @knowledgeos/search-vector-sqlite

Offline vector persistence and cosine retrieval over SQLite.

Vectors are stored as normalized JSON arrays behind a repository contract, allowing a future native vector extension without changing domain APIs.
