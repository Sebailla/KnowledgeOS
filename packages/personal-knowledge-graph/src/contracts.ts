import type {PersonalKnowledgeItem,PersonalKnowledgeRelation,PersonalKnowledgeSnapshot} from "./model.js";
export interface PersonalKnowledgeRepository { getItem(ownerId:string,itemId:string):Promise<PersonalKnowledgeItem|undefined>; upsertItem(item:PersonalKnowledgeItem):Promise<void>; listItems(ownerId:string,kinds?:readonly PersonalKnowledgeItem["kind"][]):Promise<readonly PersonalKnowledgeItem[]>; getRelation(ownerId:string,relationId:string):Promise<PersonalKnowledgeRelation|undefined>; upsertRelation(relation:PersonalKnowledgeRelation):Promise<void>; listRelations(ownerId:string):Promise<readonly PersonalKnowledgeRelation[]>; snapshot(ownerId:string,sequence:number,createdAt:string):Promise<PersonalKnowledgeSnapshot>; }
