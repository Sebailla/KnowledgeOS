import type {PersonalKnowledgeRepository} from "./contracts.js";import type {PersonalKnowledgeItem,PersonalKnowledgeRelation} from "./model.js";
export class InMemoryPersonalKnowledgeRepository implements PersonalKnowledgeRepository {private items=new Map<string,PersonalKnowledgeItem>();private relations=new Map<string,PersonalKnowledgeRelation>();private k(o:string,i:string){return `${o}::${i}`;}async getItem(o:string,i:string){return this.items.get(this.k(o,i));}async upsertItem(v:PersonalKnowledgeItem){this.items.set(this.k(v.ownerId,v.itemId),v);}async listItems(o:string,kinds?:readonly PersonalKnowledgeItem["kind"][]){return [...this.items.values()].filter(v=>v.ownerId===o&&!v.deletedAt&&(!kinds||kinds.includes(v.kind))).sort((a,b)=>a.itemId.localeCompare(b.itemId));}async getRelation(o:string,i:string){return this.relations.get(this.k(o,i));}async upsertRelation(v:PersonalKnowledgeRelation){this.relations.set(this.k(v.ownerId,v.relationId),v);}async listRelations(o:string){return [...this.relations.values()].filter(v=>v.ownerId===o&&!v.deletedAt).sort((a,b)=>a.relationId.localeCompare(b.relationId));}async snapshot(o:string,sequence:number,createdAt:string){return {ownerId:o,items:await this.listItems(o),relations:await this.listRelations(o),sequence,createdAt};}}
