# @knowledgeos/search-embedding

Embedding contracts and deterministic local embedding provider.

The deterministic provider is intended for architecture validation and offline tests. Production models can implement the same provider contract.
