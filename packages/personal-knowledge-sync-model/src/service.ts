import type { PersonalKnowledgeItem } from "@knowledgeos/personal-knowledge";import type { PersonalKnowledgeConflict,PersonalKnowledgeManifest } from "./model.js";
export class PersonalKnowledgeManifestService{create(ownerId:string,deviceId:string,generatedAt:string,items:readonly PersonalKnowledgeItem[]):PersonalKnowledgeManifest{return {ownerId,deviceId,generatedAt,entries:items.filter(i=>i.ownerId===ownerId).map(i=>({itemId:i.itemId,revision:i.revision,deleted:i.deleted,updatedAt:i.updatedAt})).sort((a,b)=>a.itemId.localeCompare(b.itemId))};}}
export class PersonalKnowledgeConflictDetector{constructor(private readonly nowIso:()=>string){}detect(baseRevision:number,local:PersonalKnowledgeItem,remote:PersonalKnowledgeItem):PersonalKnowledgeConflict|undefined{if(local.itemId!==remote.itemId)throw new Error("Different items");if(local.revision<=baseRevision||remote.revision<=baseRevision)return undefined;if(JSON.stringify(local)===JSON.stringify(remote))return undefined;return {conflictId:`${local.itemId}:${local.revision}:${remote.revision}`,itemId:local.itemId,baseRevision,local,remote,state:"conflict",detectedAt:this.nowIso()};}}
