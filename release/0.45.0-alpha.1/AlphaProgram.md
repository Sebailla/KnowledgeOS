# Closed Alpha Program

Channels: `alpha`, `rc`, `stable`.

The `alpha` channel is restricted to approved evaluators and registered devices. Diagnostic collection requires explicit consent. P0 and P1 defects block promotion to the next channel.
