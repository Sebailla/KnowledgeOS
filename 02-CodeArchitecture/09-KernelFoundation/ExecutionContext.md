# Execution Context

Execution context carries operation, correlation, actor, privacy, clock, cancellation and bounded metadata.

It is passed explicitly. Kernel code SHALL NOT depend on hidden global request state.
