@startuml 31-LocalLibraryPart4_Context
!include ../../../../00-Architecture/C4/diagrams/_includes/C4_Context.puml
title 31-LocalLibraryPart4 — Context
Person(user, "Usuario", "Opera KnowledgeOS")
System_Boundary(kos, "KnowledgeOS") {
  System(runtime, "Local Library Runtime", "Complete the durable offline execution path that moves authoritative library changes between local sqlite state, staged files, workers, and the master library without violating verified-commit ordering")
}
System_Ext(library, "Library and NAS", "Authoritative and local durable state")
Rel(user, runtime, "Uses through application APIs")
Rel(runtime, library, "Reads, writes, synchronizes or indexes through contracts")
@enduml
