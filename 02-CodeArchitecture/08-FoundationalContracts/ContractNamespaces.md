# Contract Namespaces

Contract type strings use stable namespaces:

- `library.*`
- `reading.*`
- `annotation.*`
- `search.*`
- `processing.*`
- `sync.*`
- `ai.*`
- `plugin.*`
- `export.*`

Adding an optional contract is minor-version compatible. Changing required semantics is a major-version change.
